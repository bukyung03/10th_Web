## **`gcTime`**과 **`staleTime`**의 차이점에 대해 정리해보세요! 🍠
    
    # **`gcTime`**과 **`staleTime`**의 차이점에 대해 정리해보세요 🍠
    
    <aside>
    🍠
    
    gcTime과 staleTime은 Tanstack Query에 있어서 정말 중요하게 알고 넘어가야 하는 부분이에요.
    
    **위에서 제가 설명해드렸지만 gcTime**과 **staleTime**의 개념을 다시 정리해주시고, 두 값을 어떤 식으로 설정하면 캐싱 전략에 유리한지 설명해주세요!
    
    - UMC 중앙 웹 파트장 매튜 / 김용민 - 
    
    </aside>
    
    - `gcTime`은 무엇인가요? 🍠
        
        “**메모리 청소**”를 담당한다
        
        **1️⃣ gcTime이란?**
        
        - **정의** : 컴포넌트가 언마운트(화면에서 사라짐)되어 해당 쿼리가 **사용되지 않는 (inactive) 상태**가 되었을 때, 캐시 데이터를 메모리에 얼마나 오래 보관할지 결정하는 시간이다.
        - **기본 값** : 5분
        - **이전 명칭** : `cacheTime`이라는 이름이었다. 역할이 더 명확하게 `gcTime`으로 변경되었다.
        
        **2️⃣ gcTime의 동작 원리**
        
        1. 사용자가 특정 페이지를 떠나면 해당 데이터는 `inactive` 상태가 된다.
        2. 이때부터 `gcTime` 타이머가 돌아가기 시작한다.
        3. 타이머 시간에 따라
            1. **타이머 종료 전** 다시 해당 페이지로 돌아온다면 ➡️ 캐시된 데이터를 즉시 보여준다.
            2. **타이머 종료 후** 해당 페이지로 돌아온다면 ➡️ 캐시가 삭제되었으므로 로딩 스피너 (`isPending`)를 다시 보게 된다.
        
         **3️⃣ 예시 코드**
        
        ```tsx
        const { data } = useQuery({
          queryKey: ['todos'],
          queryFn: fetchTodos,
          // 1분 동안은 데이터가 신선하다고 간주 (네트워크 요청 안 함)
          staleTime: 1000 * 60, 
          // 컴포넌트가 사라져도 10분 동안은 메모리에 저장 (다시 돌아왔을 때 깜빡임 방지)
          gcTime: 1000 * 60 * 10, 
        });
        ```
        
        **4️⃣ 유의사항**
        
        - **`gcTime`**은 **`staleTime`**과 독립적이다. `staleTime`이 아무리 길어도 `gcTime`이 지나 버리면 캐시는 삭제된다
        - **사용자 경험(UX) 최적화** : 사용자가 자주 왔다 갔다 하는 페이지라면 `gcTime`을 넉넉하게 잡아서 다시 들어와도 로딩 없이 화면이 바로 보이게 할 수 있다
        - **메모리 관리** : 데이터 양이 너무 많거나 메모리가 부족한 환경이라면 `gcTime`을 길게 잡는게 좋지 않다.
        
    - `staleTime`은 무엇인가요? 🍠
        
        “**데이터가 언제까지 신선한지(Fresh)**”를 결정하는 타임이다.
        
        **1️⃣ staleTime이란?**
        
        - **정의** : 서버에서 가져온 데이터가 **신선한 (Fresh) 상태**로 유지되는 시간이다.
        - **상태 변화** : 이 시간이 지나면 데이터는 **낡은 (Stale)** 상태가 된다
        - **기본 값** : 0 (데이터를 가져오자마자 바로 Stale 상태가 된다)
        
        2️⃣ **왜 사용하는가?**
        
        - **불필요한 네트워크 요청 감소** : `staleTime`이 1분이면 사용자가 컴포넌트를 몇번을 껐다 켜도 1분 안에 서버에 다시 물어보지 않고 캐시된 데이터를 그대로 쓴다.
        - **사용자 경험(UX) 향상** : 화면이 깜빡이지 않고 즉각적으로 데이터를 보여줄 수 있다.
        
        **3️⃣ 데이터가 Stale 해지면 생기는 일은?**
        
        데이터가 낡았다고 바로 사라지지는 않는다. 아래와 같은 **트리거가 발생했을 때** TankStack Query가 서버에서 데이터를 다시 요청(Refetch)하게 된다.
        
        1. 새로운 Query instance가 mount 될 때 (예: 페이지 이동 등)
        2. 브라우저 창에 다시 포커스가 왔을 때 (`refetchOnWindowFocus`)
        3. 네트워크가 끊겼다가 다시 연결될 때 (`refetchOnReconnect`)
        4. 설정한 `refetchInterval` 시간이 되었을 때
        
        **4️⃣ 실제 코드 예시**
        
        ```tsx
        const { data } = useQuery({
          queryKey: ['todos'],
          queryFn: fetchTodos,
          // 30초 동안은 '신선'하다고 봐줘! (30초간 재요청 금지)
          staleTime: 1000 * 30, 
        });
        ```
        
        5️⃣ **디버그**
        
        - **초록색(Fresh):** `staleTime`이 아직 안 끝난 상태
        - **노란색(Stale):** `staleTime`이 끝나서 트리거만 주면 바로 서버에 물어볼 대기하는 상태
        
        **6️⃣ gcTime vs staleTime 비교**
        
        | **비교 항목** | **staleTime (신선도)** | **gcTime (메모리 관리)** |
        | --- | --- | --- |
        | **핵심 질문** | "언제 서버에 다시 물어볼까?" | "언제 캐시를 아예 지울까?" |
        | **관심 대상** | **네트워크 비용**  | **메모리 자원** |
        | **상태 전환** | Fresh ➡️ **Stale** | Inactive ➡️ **삭제(Deleted)** |
        | **영향력** | 데이터가 신선하면 요청을 안 보냄 | 데이터가 삭제되면 로딩창을 봐야 함 |
        | **기본값** | 0 | 5분 (300,000ms) |
        | **역할** | 재요청(Refetch)을 보낼지 결정 | 메모리에서 데이터를 아예 지울지 결정 |
        | **권장값** | 보통 gcTime보다 짧게 설정 | 보통 staleTime보다 길게 설정 |
    - 두 값을 어떤 식으로 설정하여야 `캐싱 전략에 유리`한가요? 🍠
        
        **1️⃣ 기본 규칙**
        
        > **staleTime < gcTime**
        > 
        
        `gcTime`은 항상 `staleTime`보다 길어야 한다. 만약 `staleTime`이 `gcTime`보다 길면 데이터가 아직 fresh 함에도 불구하고 메모리에서 삭제되어 로딩창을 다시 봐야한다.
        
        **2️⃣ 데이터 성격에 따른 3가지 경우**
        
        **✔️ Case A : 정적 데이터**
        
        - **예시** : 설정 값, 사용자 프로필, 공지 사항, 카테고리 목록 …
        - **전략** : `staleTime`을 넉넉히 잡아 서버의 부하를 줄인다
        - **효과** : 사용자가 페이지를 이동해도 서버 요청이 거의 발생하지 않아 매우 빠르게 느껴진다.
        
        **✔️ Case B : 실시간성 데이터**
        
        - **예시** : 주식 시세, 실시간 채팅 목록, 스포츠 경기 스코어
        - **전략** : `staleTime`을 짧게 주거나 기본값(0)을 유지하여 항상 최신 상태를 보장한다
        - **효과** : 사용자가 화면을 볼 때마다 또는 포커스를 맞출 때마다 새로운 데이터를 가져와 정확한 정보를 제공한다.
        
        **✔️ Case C : 일반적인 데이터**
        
        - **예시** : 할 일 목록(Todo), 피드 포스트, 상품 리스트
        - **전략** : 사용자 경험을 위해 약 30초 ~ 1분 정도의 fresh를 유지한다.
        - **효과** : 너무 잦은 새로고침은 방지하면서 적절한 타이밍에 데이터를 동기화한다.
        
        **3️⃣ UX를 극대화**
        
        사용자가 페이지를 나갔다가 **1분 뒤에 돌아오는 상황**
        
        1. **staleTime 0, gcTime 5분:** 들어가면 `inactive` 캐시가 있으니 옛날 데이터를 보여주지만, 동시에 'Stale'하므로 배경에서 즉시 서버 요청을 보낸다. (데이터가 바뀌면 화면이 깜빡하며 업데이트 됨)
        2. **staleTime 1분, gcTime 5분:** 1분 안에 돌아왔다면 서버 요청 없이 캐시된 데이터를 그대로 쓴다. (깜빡임 전혀 없음)
        
        **✅  요약**
        
        - 서버 트래픽을 아끼고 싶다면 ➡️ `staleTime`을 늘리기
        - 사용자가 다시 돌아왔을 때 로딩바를 안 보여주고 싶다면 ➡️ `gcTime`을 늘리기
        - 항상 최신 데이터를 보여줘야 한다면 ➡️ `staleTime`을 `0`으로 두기

---
## 실제 서비스 분석
- **미션 1 - 로그인 후 화면 첫 진입 시 로딩 관찰하기**
    
    <aside>
    ❓
    
    서비스에 접속해서 각 페이지에 진입할 때 **데이터가 뜨기 전에** 무엇이 보이는지 관찰해보세요.
    
    **Q1.** **할 일 탭에** 처음 진입했을 때, 실제 할 일 목록이 뜨기 전에 어떤 화면이 보이나요?
    
    ![KakaoTalk_Photo_2026-04-04-19-33-05 001.jpeg](attachment:950e857e-a9e6-43d7-aa97-010d0d92a2e1:KakaoTalk_Photo_2026-04-04-19-33-05_001.jpeg)
    
    **Q2.** **친구 페이지**, **알림 페이지**도 각각 처음 진입해보세요. 로딩 중 화면이 서로 다른가요, 비슷한가요?
    
    ![스크린샷 2026-04-04 오후 7.57.13.png](attachment:553781ef-f7f4-4015-90f3-3932ad840469:스크린샷_2026-04-04_오후_7.57.13.png)
    
    **Q3.** 각 화면의 로딩 중 모습(스켈레톤)이 실제 콘텐츠의 **레이아웃(구조)**과 비슷한가요? 이것이 왜 존재하며 앱 사용자에게 어떤 가치를 제공할까요?
    
    </aside>
    
    - 답변
        
        <aside>
        ❓
        
        **나의 관찰**
        
        **❓할 일 탭에 처음 진입했을 때, 실제 할 일 목록이 뜨기 전에 어떤 화면이 보이나요?**
        별도의 로딩 애니메이션이나 변함 없이 한번에 노출되었다. 데이터 호출 속도가 매우 빠르거나 별도의 로딩 처리가 되어 있지 않은 상태일 것 같다.
        
        **❓친구 페이지, 알림 페이지도 각각 처음 진입해보세요. 로딩 중 화면이 서로 다른가요, 비슷한가요?**
        각 페이지의 로딩 화면이 서로 다르게 나타났다. 데이터가 불러와지기 전에 최종적으로 노출될 화면의 구조에 맞춰 회색 박스 형태의 UI (스켈레톤)이 각각 다른 레이아웃으로 미리 노출된다.
        
        **❓각 화면의 로딩 중 모습(스켈레톤)이 실제 콘텐츠의 레이아웃(구조)과 비슷한가요? 이것이 왜 존재하며 앱 사용자에게 어떤 가치를 제공할까요?**
        
        - **레이아웃 유사성** : 실제 콘텐츠가 들어갈 위치에 맞춰 회색 박스가 배치되어 실제 레이아웃과 유사한 형태를 띠고 있다.
        - **존재 이유** : 데이터가 로드 되는 동안 사용자에게 서비스가 멈춘게 아니라 데이터를 불러오고 있다는 진행 상태를 시각적으로 전달하기 위해 존재한다.
            - **사용자에게 어떤 가치를 제공하는가?**
                1. **심리적 안정감** : 아무것도 없는 빈 화면보다 대기 시간을 짧게 느끼게 하며 정상적으로 작동 중임을 인지 시켜서 이탈을 방지한다.
                2. **시각적 연속성** : 데이터가 로드된 후 레이아웃이 튀어나오는 레이아웃 시프트(Layout Shift)를 방지하여 눈의 피로도를 낮추고 부드러운 연결감을 제공한다.
                3. **예측 가능성** : 어떤 형태의 정보가 뜰지 예측할 수 있게 하여 사용자 경험을 높인다.
        </aside>
        
- **미션 2 - 화면 전환 시 캐시 동작 관찰하기**
    
    <aside>
    ❓
    
    이번에는 **화면을 왔다 갔다** 하면서, 두 번째 방문 시 로딩이 어떻게 다른지 관찰해보세요.
    
    **사전 준비**
    
    1.  카테고리가 없다면, 마이 탭 → 설정 → **카테고리** 화면에서 카테고리를 2~3개 만들어주세요.
        
        ![스크린샷 2026-04-04 오후 7.51.46.png](attachment:fc29106f-5f6e-4749-a95d-f88b838b4184:스크린샷_2026-04-04_오후_7.51.46.png)
        
    2. 할 일 탭에서 할 일도 몇 개 추가해둔다
        
        ![스크린샷 2026-04-04 오후 7.53.38.png](attachment:0086f91c-a446-4c5d-a7bb-43b58f6f97f7:스크린샷_2026-04-04_오후_7.53.38.png)
        
    3. 여기까지가 준비 단계입니다. 데이터가 준비되었으면 아래 실험을 시작하세요.
    
    **Q1. 카테고리 화면 재방문**
    
    1. 마이 탭 → 설정 → **카테고리** 화면으로 이동한다 (데이터가 로딩될 때까지 기다린다)
    2. 뒤로가기를 눌러 설정 화면으로 돌아간다
    3. **다시 카테고리 화면에 진입한다** → 로딩이 다시 나타나나요? 아니면 즉시 목록이 보이나요?
    
    **Q2. 할 일 화면 ↔ 카테고리 화면 왔다 갔다**
    
    1. 하단 탭에서 **할 일** 탭을 눌러 할 일 화면으로 이동한다
    2. 다시 마이 탭 → 설정 → **카테고리** 화면까지 돌아온다
    3. 카테고리 목록이 즉시 보이나요? 로딩이 나타나나요?
    
    ⚠️ **주의**: 실험 도중에 카테고리를 추가/수정/삭제하면 캐시가 무효화되어 다시 서버에서 가져옵니다. 캐시 동작을 관찰하려면 **아무 변경 없이 화면 이동만** 해보세요.
    
    답변을 작성 할 때 두가지 관점으로 한번 생각해보세요
    1. 만약 자신이 있는 공간이 네트워크가 느린 공간이라면?
    2. 우리가 만든 서비스가 정말 많은 인원이 쓰는 서비스인데 DB에 매초마다 정말 많은 요청을 보낸다면?
    
    </aside>
    
    - 답변
        
        <aside>
        ❓
        
        답변
        
        **Q1 답변.** 
        
        - **관찰 결과** : 첫 방문할 때는 데이터를 불러오는 로딩 과정이 있었지만 뒤로 가기 후 재진입 시에는 로딩 없이 즉시 목록이 노출되는 것을 확인했다.
        - **이유?** : 브라우저의 메모리에 이전 요청 데이터가 저장되어 서버에 다시 요청하지 않고 로컬 데이터를 사용했기 때문이다.
        ****
        
        **Q2 답변.**
        
        - **관찰 결과** : 탭을 이동하여 다른 화면을 거친 후에 다시 돌아왔음에도 불구하고 카테고리 목록이 지연 시간 없이 즉시 보였다.
        - **이유?** : React Query와 같은 라이브러리의 캐시 메커니즘이 querykey를 기준으로 데이터를 보관하기에 화면이 언마운트 되었다가 다시 마운트 되어도 기존 데이터를 즉시 활용할 수 있었다.
        
        ---
        
        **❓ 네트워크가 느린 환경이라면?**
        
        인터넷 속도가 느린 환경에서 데이터를 가져오는 데 수 초가 걸릴 수 있다. 캐싱이 없다면 사용자는 화면을 바꿀 때마다 매번 긴 로딩을 견뎌야 하지만 캐싱을 활용하면 오프라인에 준하는 속도로 화면 전환이 가능해져 사용자 이탈을 막을 수 있다.
        
        **❓ 매 초마다 많은 요청을 보낸다면?**
        
        - **서버 자원 절약:** 많은 사용자가 매초마다 목록 보기를 요청한다고 가정할 때, 캐싱이 없다면 DB는 1초에 수만 번의 조회(Select) 쿼리를 처리해야 한다. 즉, 서버 다운이나 비용 상승으로 이어진다.
        - **효율적인 트래픽 관리:** 한 번 가져온 데이터를 캐시에서 꺼내 쓰게 되면 서버로 가는 요청 수 자체를 획기적으로 줄일 수 있어, DB 부하를 줄이고 서비스의 전체적인 안정성을 높일 수 있다
        </aside>
        